# 🧭 Lesson 1：TypeScript 是什麼？

TypeScript ( 簡稱 TS ) 是 **JavaScript 的超集合 (superset)**

- 加入了 **靜態型別 (type checking)**
- 最後 **編譯成 JavaScript**

---

# 🧩 安裝與使用

**✅ 方法 1：在瀏覽器玩（不用安裝）**
👉 開啟 https://www.typescriptlang.org/play

在那裡貼上範例就能即時看到結果。

**✅ 方法 2：在本機安裝（Node.js）**

```bash
npm install -g typescript
tsc --init
```

然後輸入：

```bash
tsc index.ts
node index.js
```
